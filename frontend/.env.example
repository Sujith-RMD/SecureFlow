# Copy this file to .env.local for local dev (leave empty — Vite proxy handles it)
# For production builds, set this to your deployed backend URL:
VITE_API_BASE_URL=https://your-backend.onrender.com
